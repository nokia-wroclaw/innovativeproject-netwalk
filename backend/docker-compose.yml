services:
  timescaledb:
    image: timescale/timescaledb:latest-pg15
    container_name: ZPI2026
    environment:
      - POSTGRES_USER=netwalk
      - POSTGRES_PASSWORD=niestandard123walk
      - POSTGRES_DB=pomiary
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: